# En tu repo de GitHub: auth.yml
server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth
    username: reservs
    password: reservs
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
